{
  "name": "random-array-subset",
  "version": "1.0.0",
  "description": "Returns an array of a specified length containing a random subset of elements from a given array",
  "main": "index.js",
  "scripts": {
    "test": "node ./test"
  },
  "keywords": [
    "random",
    "array",
    "fill"
  ],
  "author": "Aaron Morris",
  "license": "MIT",
  "devDependencies": {
    "tape": "^4.2.0"
  }
}
